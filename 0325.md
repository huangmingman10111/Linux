# Linux 作業系統實務筆記 (Practice of Linux Operating System)

---

## 一、 Linux 簡介與基礎架構
* **起源與精神**：1991 年由 Linus Torvalds 開發。主打開放原始碼 (Open Source)，遵循 GPL 授權。
* **吉祥物**：Linux 的代表圖標是一隻 **企鵝**。
* **核心觀念**：**一切皆檔案 (Everything is a file)**。硬體、目錄、檔案在系統中皆一視同仁。
* **架構層次 (會考)**：**Kernel (核心)** > **Shell (殼層)** > **Application (應用程式)**。
  * **核心 (Kernel)**：系統心臟，負責硬體溝通與資源配置。
* **本學期版本 (會考)**：**ubuntu 24.04** (LTS)。
* **發行版體系**：
  * **Debian 體系**：如 **Ubuntu**。使用 **`APT`** 管理軟體。
  * **RedHat 體系**：如 RHEL、CentOS。使用 **`YUM`** 或 **`DNF`**。

---

## 二、 目錄、檔案與路徑操作

### 1. 路徑符號 (會考)
* **`.`**：代表 **本地目錄 (Local Directory)**。
* **`..`**：代表 **上層目錄 (Upper Directory)**。
* **`~`** (Tilde)：代表 **家目錄 (Home Directory)**。
* **`/`**：代表 **根目錄 (Root Directory)**，系統最頂層。

### 2. 檔案系統目錄詳解 (會考)
* **`/etc`**：存放 **系統設定檔** (如網路、IP、DNS、伺服器配置)。
* **`/var`**：存放 **變動資料 (Variable)** (如 Log 紀錄檔、網頁家目錄 `/var/www/html`)。
* **`/tmp`**：**暫存區**。任何使用者皆可在此寫入或下載檔案。
* **`/dev`**：**設備檔 (Device)**。存放對應硬體（鍵盤、硬碟）的檔案。
* **`/bin`**：一般使用者的執行檔。
* **`/sbin`**：管理者 (Superuser) 專用的執行檔。
* **注意**：Linux 沒有 C 槽、D 槽之分，採樹狀結構，可將不同硬碟掛載到特定資料夾。

### 3. 指令實務
* **隱藏概念 (會考)**：檔名開頭有 **`.`** 的是隱藏檔 (例如 `.env`)。
* **ls 指令 (會考)**：**`ls -al`** (或 `ls -la`, `ls -a -l`) 可看隱藏檔。
  * **`ls -h`**：以人類易讀格式 (1K, 2M) 顯示大小。
* **`pwd`**：顯示目前所在位置。
* **`touch`**：快速產生一個空白檔案（常用於測試）。
* **相對路徑複製範例**：`cp ./a ../../tmp/b`（將目前目錄下的檔案 a，複製到往上跳兩層後的 tmp 資料夾內並更名為 b）。

---

## 三、 使用者身分與權限管理 (核心考點)

### 1. 身分辨識
* **系統管理者 (會考)**：名稱叫 **root**，其 **ID (UID) 是 0**。
* **提示符號辨識 (會考)**：
  * 一般使用者提示符號是 **`$`**
  * 管理者提示符號是 **`#`**
* **提權指令 (會考)**：使用 **`sudo su`** 變換身分為管理者。

### 2. 帳號管理指令
* **身分查詢**：`whoami` (看自己是誰)、`w` (看誰在線上與做什麼)、`id` (看 UID)。
* **新增/刪除**：`useradd` (新增)、`userdel` (刪除)。
* **密碼邏輯 (會考)**：執行 `sudo` 時，輸入的是 **「使用者自己的密碼」**，而非 root 密碼。
* **切換使用者**：`su - [帳號]` (完整載入環境變數)。
* **環境修復**：若切換後 `ls` 沒反應，輸入 `sudo usermod -s /bin/bash [使用者]` 修正。

### 3. 檔案權限判讀 (ls -l)
* **第 1 碼 (會考)**：類型。**`-` 為檔案，`d` 為目錄**。
* **2-10 碼**：每 3 碼為一組 (**擁有者 / 群組 / 其他人**)。
* **代號含意**：`r` (讀)、`w` (寫)、`x` (執行)。
* **修改權限**：`chown` (改擁有者)、`chmod` (改權限)。

---

## 四、 VIM 文字編輯器與伺服器管理

### 1. VIM 三種模式
* **一般模式**：預設進入，僅能瀏覽。
* **插入模式**：按 **`i`** 或 **`a`** 進入，左下角顯示 `-- INSERT --` 始可打字。按 **`ESC`** 退出。
* **命令模式 (會考)**：一般模式下按 **`:`** (冒號)。
  * **`:w`**：儲存。
  * **`:q`**：離開。
  * **`:wq`**：儲存並離開。
  * **`:q!`**：強制離開不儲存。

### 2. 伺服器與連線
* **SSH**：遠端連線。需 `sudo ufw allow ssh` 防火牆放行。
* **Apache**：網頁伺服器。路徑為 `/var/www/html/`。
* **狀態檢查 (會考)**：檢查服務是否顯示綠色 **`active (running)`**。
* **cURL**：文字版瀏覽器，可用於自動化檢測伺服器是否正常運作。

---

## 五、 進階技巧與操作小撇步

* **指令取代 (會考)**：使用 **反引號 `` `指令` ``** 或 **`$(指令)`**。先執行內部指令再傳回結果。
* **環境變數 (會考)**：在 `.env` 檔案中，**金鑰引號必須成對** (例如 `TOKEN="key"`)。
* **管道與過濾**：
  * **`|`**：將前一個指令結果傳給下一個指令。
  * **`grep`**：過濾文字（`grep -v` 排除符合行）。
* **操作快捷鍵**：
  * **Ctrl + C**：強制終止執行中的程式 (畫面卡住時必用)。
  * `Ctrl + O` / `Ctrl + X`：Nano 編輯器存檔與離開。
  * **上鍵 (↑)**：快速翻閱歷史指令。
* **危險指令 (rm)**：**`rm -rf *`** 會強制刪除所有檔案且不詢問，使用時必須極度小心。
