# Linux 作業系統實務筆記 (Practice of Linux Operating System)

---

## 一、 Linux 簡介與基礎架構
* **起源與精神**：1991 年由 Linus Torvalds 開發，遵循 GPL 授權（開源、原始碼公開）。
* **吉祥物**：Linux 的代表圖標是一隻 **企鵝**。
* **核心觀念**：**一切皆檔案 (Everything is a file)**。硬體、目錄、檔案在系統中皆一視同仁。
* **本學期版本 (會考)**：**Ubuntu 24.04 LTS** (長期支援版)。
* **發行版體系**：
  * **Debian 體系**：如 **Ubuntu**。使用 **`APT`** 管理軟體。
  * **RedHat 體系**：如 RHEL、CentOS。使用 **`YUM`** 或 **`DNF`**。
* **架構層次 (會考)**：**Kernel (核心) > Shell (殼層) > Application (應用程式)**。
  * **核心 (Kernel)**：系統心臟，負責與硬體溝通、配置記憶體、資源管理。
  * **殼層 (Shell)**：如 bash、tcsh。輸入 `exit` 或 `Ctrl+D` 可回到原本的殼。

---

## 二、 檔案系統目錄架構 (核心考點)
Linux 採 **樹狀結構**，沒有 C 槽、D 槽之分，所有東西都從 **根目錄 `/`** 開始。

* **`/etc` (會考)**：存放 **系統設定檔**（如主機、網路 IP/DNS、伺服器配置）。
* **`/var` (會考)**：存放 **變動資料 (Variable)**（如 Log 紀錄檔、網頁家目錄 `/var/www/html`）。
* **`/tmp` (會考)**：**暫存區**。權限完全開放，**任何使用者**皆可在此寫入或下載檔案。
* **`/dev` (會考)**：**設備檔 (Device)**。存放對應硬體（鍵盤、滑鼠、硬碟）的檔案。
* **`/bin`**：一般使用者的執行檔 (Binary)。
* **`/sbin`**：管理者 (Superuser) 專用的執行檔。
* **`/boot`**：開機相關檔案。
* **`/opt`**：使用者自行安裝的第三方應用程式。
* **補充觀念**：可將不同硬碟「掛載 (Mount)」到特定資料夾，使用者只會看到資料夾而非磁碟代號。



---

## 三、 目錄、檔案與路徑操作

### 1. 路徑符號 (必考)
* **`.`**：代表 **目前目錄 (Local Directory)**。
* **`..`**：代表 **上一層目錄 (Upper Directory)**。
* **`~`** (Tilde)：代表 **家目錄 (Home Directory)**。執行 `cd` 或 `cd ~` 效果相同。
* **`/`**：代表 **根目錄 (Root Directory)**。

### 2. 基礎指令實務
* **隱藏概念 (會考)**：檔名開頭有 **`.`** 的是隱藏檔 (例如 `.env`)。
* **ls 指令 (會考)**：**`ls -al`** (或 `ls -la`, `ls -a -l`) 可看隱藏檔。
  * **`ls -h`**：以人類易讀格式 (1K, 2M) 顯示大小。
* **`pwd`**：顯示目前所在位置的路徑。
* **`hostname`**：顯示主機名稱。使用 `hostname -I` 快速查 IP。
* **`touch`**：快速建立一個空白檔案。
* **相對路徑複製範例**：`cp ./a ../../tmp/b`（將目前目錄下的 a 複製到上上層的 tmp 資料夾並改名為 b）。

---

## 四、 使用者身分與權限管理 (超級考點)

### 1. 身分辨識與管理
* **系統管理者 (會考)**：名稱叫 **root**，其 **ID (UID) 是 0**。
* **提示符號辨識 (會考)**：
  * 一般使用者提示符號是 **`$`**
  * 管理者提示符號是 **`#`**
* **提權指令 (會考)**：使用 **`sudo su`** 變換身分為管理者。
* **密碼邏輯 (會考)**：執行 `sudo` 時，輸入的是 **「使用者自己的密碼」**，而非 root 密碼。
* **查詢指令**：`whoami` (看自己是誰)、`w` (看誰在線上與做什麼)、`id` (看 UID)。
* **帳號指令**：`useradd` (新增)、`userdel` (刪除)、`passwd [帳號]` (改密碼)。
* **切換使用者**：`su - [帳號]` (完整載入環境變數)。

### 2. 檔案權限判讀 (ls -l)
當看到 `-rwxr-xr-x` 時：
* **第 1 碼 (會考)**：類型。**`-` 為一般檔案，`d` 為目錄**。
* **2-10 碼**：每 3 碼為一組 (**擁有者 Owner / 群組 Group / 其他人 Others**)。
* **代號含意**：**`r` (讀)、`w` (寫)、`x` (執行)**、`-` (無權限)。
* **修改指令**：`chown` (改擁有者)、`chmod` (改權限)。



---

## 五、 VIM 文字編輯器與伺服器服務

### 1. VIM 三種模式 (實作重點)
* **一般模式 (Normal)**：預設進入，僅能瀏覽。
* **插入模式 (Insert)**：按 **`i`** 或 **`a`** 進入，左下角顯示 `-- INSERT --` 始可打字。按 **`ESC`** 退出。
* **命令模式 (Command) (會考)**：一般模式下按 **`:`** (冒號)。
  * **`:w`**：儲存。
  * **`:q`**：離開。
  * **`:wq`**：儲存並離開。
  * **`:q!`**：不儲存強制離開。

### 2. 伺服器與服務管理
* **SSH**：遠端連線用。需 `sudo ufw allow ssh` 防火牆放行。
* **Apache (`apache2`)**：網頁伺服器。預設路徑 `/var/www/html/`。
* **狀態檢查 (會考)**：檢查服務是否顯示為綠色 **`active (running)`**。
* **cURL**：文字版瀏覽器，可用於自動化檢測伺服器是否正常（抓不到東西即異常）。

---

## 六、 進階技巧與操作小撇步

### 1. 指令進階語法
* **指令取代 (會考)**：使用 **反引號 `` `指令` ``** 或 **`$(指令)`**。先執行內部指令再傳回結果。
  * 範例：`echo $(hostname -I) > index.html`。
* **環境變數 (會考)**：在 `.env` 檔案中，**金鑰引號必須成對** (例如 `TOKEN="key"`)。
* **管道與過濾**：
  * **`|`** (Pipe)：將前一個指令結果傳給下一個指令（如生產線）。
  * **`grep`**：過濾文字（`grep -v` 排除符合行）。

### 2. 系統安全與快捷鍵
* **快照 (Snapshot)**：虛擬機紀錄點。在進行危險實驗前務必先做快照，出錯可隨時還原。
* **危險指令 (rm)**：**`rm -rf *`** 會強制遞迴刪除所有檔案且不詢問，極度危險。
* **快捷鍵**：
  * **Ctrl + C**：強制終止執行中的程式 (畫面卡住時必用)。
  * **上鍵 (↑)**：快速翻閱歷史指令紀錄。
  * **Tab 鍵**：自動補齊指令或檔名。
* **電源指令**：`poweroff`、`shutdown -h now`。
* **TTY/PTS**：
  * **TTY**：伺服器本機虛擬終端 (`Ctrl+Alt+F3~F6` 切換，`F1` 回圖形介面)。
  * **PTS**：遠端連線或視窗內的終端機。

---

## 七、 網路觀念
* **第二層 (L2)**：交換器 (Switch)，連接同一個區域網路 (LAN)，會傳送廣播。
* **第三層 (L3)**：路由器 (Router)，連接不同區域網路，會隔離廣播。
* **CLI vs GUI**：CLI 的好處是 **速度快** 且能實現 **自動化**（如搶票腳本）。
