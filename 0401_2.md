# 🐧 Linux 基礎與權限管理：完整學習筆記 (含進階操作)

## 一、 目錄切換與建立 (`cd` & `mkdir`)
* **`cd` (Change Directory)**：
    * `cd .`：停留在目前的目錄。
    * `cd ..`：回到上一層目錄（回上一層）。
    * `cd /`：到根目錄（到）。
    * `cd ~`：回到家目錄（回家）。
    * `cd -`：回到前一個停留的目錄（前目錄）。
* **`mkdir` (Make Directory)**：
    * `-d`：常用於測試目錄是否存在。
    * `-p` (Parents)：直接建立。即使中間的資料夾不存在，也會一併建立。

---

## 二、 檔案管理與複製 (`cp` & `rm`)
* **`cp` (Copy)**：把檔案複製到另一個地方。
    * `+/位置`：複製到某位置。
    * `+-v`：顯示複製過程 (Verbose)。
    * `-r`：複製目錄（r 代表遞迴 Recursive）。
    * **`*` (Wildcard)**：萬用字元，代表所有目錄與檔案。
* **`{}` 擴充 (Brace Expansion)**：
    * 範例：`touch {1..9}.txt` (一次建立 1.txt 到 9.txt)。
* **`rm` (Remove)**：
    * `rm -rf 檔案/目錄`：刪除檔案。
    * **`-r`**：遞迴刪除。
    * **`-f`**：強制刪除。



---

## 三、 別名設定與環境配置 (`alias` & `.bashrc`)
* **`alias` (別名)**：
    * `alias [別名]=[指令]`：在當前視窗取別名。
    * 直接輸入 `alias`：查詢目前有哪些別名。
    * `unalias`：取消當前視窗的別名。
    * **`\` (反斜線)**：臨時取消 alias（執行原始指令）。
* **進入 `vim .bashrc` 更改**：直接更改殼 (Shell) 的設定檔。
* **Vim 基本退出操作**：
    * 先按 **`Esc`** 鍵。
    * **`:q!`**：不儲存強制退出 (Quit without saving)。
    * **`:wq`**：儲存並退出 (Write and Quit)。

---

## 四、 檔案檢視工具 (`cat`, `less`, `head`, `more`)
* **`cat`**：查看檔案裡面的內容。
    * `cat -n /etc/passwd | more`：顯示行號並分頁查看使用者帳密檔。
* **`more`**：分頁查看內容（只能往下翻）。
* **`less`**：進階分頁查看（可往上、下翻，效能較好）。
* **`head`**：查看檔案開頭的前幾行。



---

## 五、 基本指令與環境配置
* **常用 ls 組合**：`ls + -a \ -s \ -h \ /tmp`
* **隱藏檔案**：`.` 開頭的檔案為隱藏檔，須加 `-a` 才會顯示。
* **`$` (取值符號)**：把值取出來。

---
![ls 與變數取值圖片](https://github.com/user-attachments/assets/ca275b08-57fa-43b4-8cb0-36c7a22ebdf0)

---

## 六、 系統環境與執行路徑
* **`env`**：查看系統環境變量。
* **`echo $PATH`**：印出搜尋執行檔的路徑。
* **`which`**：查詢執行檔所在的位子（一般檔案無法查詢）。

![which 查詢範例](https://github.com/user-attachments/assets/6106be29-dd83-40b4-b534-55849d983fb9)

---

## 七、 檔案權限詳解 (`ls -l`)
* **類型標示**：`d` > 目錄 | `c` > 字元設備 | `b` > 區塊設備。
* **權限結構**：`---owner ---group ---others`
* **rwx 意義**：`r` > read | `w` > write | `x` > execute。
* **`chmod` (Change Mode)**：使用 `+` 增加或 `-` 移除權限。

![chmod 權限修改範例](https://github.com/user-attachments/assets/96ac69ed-8dd9-439a-b100-01e77f770f52)

---

## 八、 檔案操作與狀態
* **`>` (重導向)**：例如 `echo hi > hi.txt`。
* **`history`**：查詢指令歷史紀錄（`!數字/關鍵字`）。
* **`tree`**：顯示資料夾樹狀結構。
* **`stat` (檔案狀態)**：查看 Access, Modify, Change 時間。

![stat 檔案狀態圖片](https://github.com/user-attachments/assets/542820d9-7172-40a9-9add-099ab2839f31)

---

### 📖 英文小講堂 (English Learning)

1.  **Wildcard** [ˈwaɪldkɑːrd]: 萬用字元（在 Linux 中就是 `*`）。
2.  **Verbose** [vɜːrˈboʊs]: 冗長的、詳細的（`-v` 就是讓電腦「話多一點」告訴你過程）。
3.  **Alias** [ˈeɪliəs]: 別名、化名。
4.  **Append** [əˈpend]: 附加（`>` 是覆蓋，`>>` 是 **Append**）。
5.  **Recursive** [rɪˈkɜːrsɪv]: 遞迴的（`-r` 的全稱）。
