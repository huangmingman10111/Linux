# 資安工具與 Docker 資料持久化筆記

---

## 一、 網路偵察工具：Nmap (Network Mapper)
**Nmap** 是一款開源的網路掃描工具，用於探索網路上的主機與服務，是資安檢測的第一步。

* **核心功能：**
    * **主機發現 (Host Discovery)**：確認目標主機是否在線。
    * **連接埠掃描 (Port Scanning)**：偵測目標開啟了哪些服務（如 SSH:22, HTTP:80）。
    * **服務版本偵測 (-sV)**：識別目標服務的具體版本資訊。
    * **作業系統偵測 (-O)**：推測目標運作的系統（Linux, Windows 等）。
* **常用指令範例：**
    ```bash
    nmap -sV 192.168.1.1  # 掃描並偵測服務版本
    nmap -A 192.168.1.1   # 強力掃描（含 OS 偵測、腳本掃描、路由追蹤）
    ```

---

## 二、 密碼破解工具：Hydra
**Hydra** 是一款強大的平行化登入破解工具，支援多種協定（SSH, FTP, HTTP, MySQL 等）。

* **運作原理：**
    使用「字典檔（Dictionary Attack）」進行暴力破解。透過預先準備好的使用者名單與密碼名單，自動化地嘗試登入。
* **常用參數說明：**
    * `-L`：指定使用者名稱檔案。
    * `-P`：指定密碼清單檔案。
    * `-t`：設定執行緒（並行任務數量），數量越多速度越快，但容易被伺服器阻斷。
* **指令範例：**
    ```bash
    hydra -L user.txt -P pass.txt ssh://192.168.1.1 -t 4
    ```

---

## 三、 Docker 資料持久化 (Persistence)
Docker 容器預設是 **「非持久化（Ephemeral）」** 的。一旦容器被 `docker rm` 刪除，所有在容器內新增或修改的內容都會消失。

若要保留資料，必須採用以下兩種方法：

### **方法一：掛載本地磁碟 (Volume / Bind Mount)**
在啟動容器時，使用 `-v` 參數將宿主機（Host）的資料夾映射（Mapped）到容器內部。
* **指令：** `docker run -d -v /home/user/data:/usr/local/apache2/htdocs httpd`
* **細節補充：**
    * **同步特性**：兩邊資料即時同步。在宿主機修改檔案，容器內會立刻生效。
    * **生命週期**：即使容器刪除，宿主機上的檔案依然存在，下次啟動新容器再掛載即可恢復資料。
    * **最推薦做法**：適合存放資料庫內容、網頁原始碼等常動態變動的資料。

### **方法二：提交映像檔快照 (Docker Commit)**
將目前運作中、已經修改過的容器，直接「封裝」成一個新的映像檔（Image）。
* **指令：** `docker commit [容器ID] my-custom-image:v1`
* **細節補充：**
    * **快照概念**：類似遊戲的「存檔」。它將目前的檔案系統狀態拍一張照片保存下來。
    * **使用場景**：適合當你在容器內安裝了複雜的套件或調整了深層設定，想把這個環境「固化」下來給以後使用。
    * **缺點**：映像檔會變得越來越臃腫，且無法追蹤修改過程，建議僅用於暫時保存環境。

