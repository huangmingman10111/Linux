# Linux 基礎指令筆記

## 1. 查看檔案內容
* **`cat`**：直接印出檔案所有內容。
* **`more`**：分頁查看內容（分段閱讀）。
    * `cat /etc/passwd | more`
    * `more /etc/passwd`
* **`less`**：功能比 more 強，可自由上下捲動。
    * `less /etc/passwd`（建議用法，按 `q` 離開）
    * `cat /etc/passwd | less`（管線用法，通常只能往下翻）
* **`head`**：查看檔案開頭（預設前 10 行）。
* **`tail`**：查看檔案結尾（預設後 10 行）。
    * `tail -f 檔案名`：**f (follow)**，實時追蹤檔案更新，常用於查看 Log。

## 2. 重新導向與合併
* **`> b`**：清空 b 檔案內容（或建立空檔案）。
* **`echo aaaaa > a`**：將 aaaaa 寫入檔案 a（覆蓋）。
    * `1>` 同 `>`，代表標準輸出 (Standard Output)。
* **`>>`**：**追加**，將內容加到檔案末尾而不覆蓋原內容。
* **`cat 1.txt 2.txt > 3.txt`**：將 1 和 2 的內容合併後轉存到 3。

## 3. 檔案操作與路徑
* **`mv` (move)**：搬移或改名。
    * `mv a.txt ..`：將檔案搬移到上一層目錄。
    * `mv z1.txt z3.txt`：將 z1 改名為 z3。
* **`pwd`**：顯示當前路徑（Print Working Directory），等同於 `echo $PWD`。
* **`touch`**：建立空檔案或更新檔案時間。

## 4. 搜尋檔案
* **`locate`**：利用資料庫搜尋檔案，速度快。
    * `locate 關鍵字`：搜尋包含關鍵字的檔案。
* **`which`**：搜尋執行檔（指令）的完整路徑。
* **`find`**：在硬碟中直接搜尋，功能最強但速度較慢。
    * `find . -name "*.php"`：搜尋副檔名為 php 的檔案（模糊匹配需加引號 `""`）。
    * `find . -type d`：搜尋**目錄 (Directory)**。
    * `find . -type f`：搜尋**一般檔案 (File)**。
    * `find . -type f ! -perm 777`：尋找權限**不是** 777 的檔案。
    * `find . -type f -perm 777 -exec chmod 644 {} \;`：找到 777 的檔案並將其權限改為 644。
    * `-size`：指定檔案大小搜尋。

## 5. 其他重要觀念
* **`grep`**：過濾器，從文字中篩選出想要的關鍵字。
* **大小寫區分**：Windows 不區分，**Linux 嚴格區分大小寫**。
* **檔案時間戳記**：
    * **atime (access time)**：存取時間，檔案被讀取（如 `cat`）時更新。
    * **mtime (modify time)**：內容修改時間。
    * **ctime (change time)**：狀態變動時間（內容改變或權限變動）。

## 6. 系統管理
* **關機 (Shutdown)**：
    * `shutdown -h`：關機。
    * `shutdown -r`：**reboot (重啟)**。
    * `halt -p` / `poweroff`：停止運行並關機。
* **登出**：`logout`。
* **說明文件**：`man 指令`（**man**uscript，查詢指令使用手冊）。
  ---
  ---
  ---
  ---
  
  
  
  
  
  
  
  
  
  

* 查看檔案內容
  * cat
  * more 幫忙分頁
    1. cat /etc/passwd |more
    2. more /etc/passwd
  * less 往上 往下
    1. cat /etc/passwd |less 只能往下
    2. less /etc/passwd
  * head 
  * tail
    * tail -f 檔案名 f(follow)
* />b 清空b檔案
* /echo aaaaa > a = /echo aaaaa 1> a
* cat 1.txt 2.txt >3.txt 合併檔案內容
* />> 追加
* mv (move)
  * ~/bakdir/backup$ mv a.txt ..將a.txt檔案搬移到上一層
  * ~/bakdir/backup$ mv z1.txt z3.txt 改檔名
* pwd 顯示當前路徑 = echo $PWD
* locate 找執行檔和一般的文件檔(較快)
  * locate 關鍵字 > 搜尋出有關鍵字的檔案
* which 搜尋檔案
* find 在硬碟裡搜尋檔案(速度慢功能強)
  * [https://blog.gtwang.org/linux/unix-linux-find-command-examples/]
  * find -name在用的時候,如果有加上*,要加上""進行模糊匹配,不然就是完全匹配
    * 在命令列中 / * 為萬用字元(整個名稱加"")
    * 找副檔案 / find . -type f -name "*.php"
    * {} \ 找到的檔案的集合
  * 指定檔案類型
    * -type d 目錄
    * -type f 一般檔案
  * 檔案權限
    * find . -type f ! perm 777 尋找權限不是(i)777的檔案
    * find . -type f perm 777 -exec chmod 644 {} \; 找到權限為777的檔案，執行chmod，把權限改成644
  * 指定檔案修改與存取時間
  * 指定檔案大小-size
* grep 過濾器
* windows不區分大小寫，linux區分
* touch 建立空檔案
* 檔案時間
  * atime:acess time存取時間 有被讀取就會被修改(cat)
  * mtime:modify time內容修改
  * ctime:change time內容變動or權限變動
* 關機
  * shutdown -h
    * -r reboot重啟
  * halt -p halt(停止)
  * poweroff
* 登出 logout
* man ls 操作指令 man(manusript手冊)
